# LED POP QA — 2026-09-10

## 대상과 범위

Galaxy SM-M336K, Android 16, 1080×2408, 기존 density override 480, 3버튼 내비게이션. 설치 앱은 main 06f8847의 production APK 1.0.6 (24)입니다. SHA-256 `209792b8fa1f4933f12ac7d17555efad3a1a56fe1703f5de90da6abc4ca9be6a`가 [빌드 원본](../artifacts/apk-runs/20260910-170358-6aecc7ad/BUILD.md)과 일치합니다.

이 보고서의 최초 작성 작업은 QA·테스트·수정입니다. 새 컴파일/APK/AAB·커밋·푸시·업로드는 실행하지 않았고 별도 RootLayout 스플래시 수정은 보존했습니다. 테스트 후 원래 두 줄 입력, 영어/Montserrat, 해상도 및 Wi-Fi 켜짐을 확인했습니다.

## 실기기 결과

| 항목 | 결과 |
|---|---|
| 전체 화면/홈 이동 후 복귀 | 원본 두 줄 입력 유지 |
| Settings | V1.0.6, Upgrade to Pro 숨김 |
| Sunny 목록/하단 | decibella 2가 decibella 위, 아이콘 표시, 로고 왼쪽·Terms/Privacy 오른쪽 |
| 7개 언어 모달 | 모든 언어 UI 텍스트/bounds 수집, 영어·한국어·일본어·프랑스어 대표 화면 확인 |
| 작은 화면 | 임시 1080×1440에서 스크롤로 버튼 도달. 본문·버튼·닫기·이미지·여백 드래그 중 실행 없음. 실제 태블릿 검증은 아님 |
| Blink | 빈도 컨트롤 표시/해제, 원래 꺼짐 복원 |
| 한국어 광고 | 지정 준비/실패 문구, 비활성 `광고 준비 중…` 확인 |
| 수동 재시도 | 최종 실패에만 제공, 완료 후 동일 버튼 영역에서 활성화, 자동 재생 없음 |
| 리워드 실패 | 3회 후 중단, 화면 재진입/언어 변경만으로 재요청 없음 |
| 배너 실패 | 초기 3회와 약 70초 뒤 추가 3회. 추가 실패 후 약 62초 관찰 동안 추가 요청 없음 |

배너 SDK 실패 콜백: 18:56:55.318 → 18:57:01.472 → 18:57:13.612 → 18:58:23.743 → 18:58:29.929 → 18:58:42.031. 현재 구현은 실패 후 **6초·12초**이며 이전 3초·6초 기록과 구분합니다. 콜백 간격에는 SDK 처리 시간이 포함됩니다. 안내는 약 3.4초 간격 UI 폴링에서 약 10.8초로 관찰됐고 정확한 10초 타이머는 자동화 테스트로 검증했습니다. 오프라인 SDK 오류 코드는 0이며 세부 원인은 추정하지 않았습니다.

## 성능

Perfetto FrameTimeline/process_stats로 각각 30초씩 3회, 두 줄 Montserrat/자간 24 조건에서 측정했습니다.

| 지표 | 미리보기 | 전체 화면 |
|---|---:|---:|
| 프레임 타임라인 시작 수/초 | 120.08–120.09 | 120.08–120.09 |
| 시작 간격 P95 | 8.529–8.553 ms | 8.560–8.576 ms |
| 프레임 수명 P95 | 11.555–11.586 ms | 7.213–7.272 ms |
| deadline miss/30초 | 0 / 0 / 0 | 28 / 0 / 15 |
| 프로세스 PSS | 604.95–606.28 MiB | 720.66–730.47 MiB |

전체 화면에서 숨겨진 미리보기 레이어 프레임은 0이며 전체 화면 레이어 하나만 동작했습니다. 미리보기 BufferStuffing 분류는 남습니다. 시작 수는 실제 표시 FPS나 React 렌더 횟수가 아니며 프레임 수명은 순수 JS/CPU 시간이 아닙니다.

다국어 UI 순회 후 warm 측정이며 별도 cold restart PSS는 318.92 MiB였습니다. 메모리 증가 원인/누수는 확정하지 않았습니다. 이전 QA와 입력 조건이 달라 개선율은 계산하지 않았습니다. React render/commit 횟수는 release APK에서 측정하지 못했습니다. 후속 조사 우선순위는 동일 조건 반복 전환의 메모리 추세와 BufferStuffing 원인 확인입니다.

## 발견한 문제와 수정

`ads/rewardedState.ts`: 네이티브 표시 준비를 기다리는 동안 광고가 만료되거나 loaded 상태가 해제돼도 show가 호출될 수 있었습니다. await 이후 loaded/loadedAt/유효 기간을 재확인하도록 최소 수정했습니다. 재현 테스트는 수정 전 실패, 수정 후 통과했습니다. **수정은 소스에만 있으며 설치 APK에서 재검증하지 않았습니다.**

`node --test scripts/qa-ad-state.cjs`: 21개 통과. 실제 상태 모듈을 로드하고 SDK 경계·시계만 격리합니다. 배너 제한/재진입/refresh, 보상 중복/취소/만료/이벤트, SDK 초기화, 웹 이벤트 순서/비지원, production 콘솔 억제, 프리셋 복사/입력 보존을 다룹니다. 제품에 테스트 대역이나 보상 우회는 추가하지 않았습니다.

## 미검증

- AdMob 테스트 기기 등록 여부가 확인되지 않아 실제 rewarded 재생·닫기·보상 지급은 실행하지 않았습니다. 광고 클릭/설치도 하지 않았습니다.
- 광고 진입/복귀 내비게이션 바 단일 프레임 노출, gesture 모드, iOS와 실제 태블릿은 미검증입니다.
- 잠금 해제 후 모든 배경 효과/프리셋 경로, 실시간 Amplitude 수신, 웹 브라우저 전체 UI는 미검증입니다. 상태 테스트를 실기기 검증으로 대체하지 않습니다.

## 증빙

[로컬 QA 산출물](../artifacts/qa-20260910/): performance-summary.json, Perfetto traces/CSV, locale-modal-results.json, banner-ui-timeline.json, offline-final.log, 한국어 상태 XML/PNG, restored-main.xml, state-regression.log. 로그의 원본 식별자를 문서로 옮기지 않았습니다.

## 메모리·프레임 추가 조사

[20회 반복 측정 및 표시 시각 분석](QA_MEMORY_FRAMES_20260910.md): 초기 증가분은 주로 GL 그래픽 메모리이고 이후 안정화됐습니다. 지속 누수는 확인하지 못했으나 그래픽 메모리 보유량은 큽니다. BufferStuffing 구간도 표시 간격은 일정했고 한 표시 주기의 추가 대기와 부합했습니다. 앱 코드 변경 없이 조사·문서화했습니다.

## main 전달 범위

사용자의 후속 요청으로 QA 문서, 광고 표시 직전 만료/준비 상태 재확인, 화면 수명의 picture/paint 재사용, 동일 크기 레이아웃 갱신 억제, 회귀 테스트 파일을 main 커밋·푸시 대상으로 정리했습니다. 위의 커밋·푸시 미실행 문구는 각 조사/구현 시점의 기록입니다. 별도 RootLayout 스플래시 수정은 전달에서 제외하고 로컬에 보존합니다. 원본 측정 파일은 artifacts 아래 로컬에 남아 있으며 Git 전달에는 보고서와 테스트 소스가 포함됩니다. 후속 소스의 새 APK 빌드와 실기기 A/B 비교는 여전히 미실행입니다.

## 2026-09-21 — V1.1.2 (30) 후속 실기기 QA

- 대상: Galaxy SM-M336K / Android 16. 설치 base.apk SHA-256 `0C56845470CB761E7825712E88441B308EA2591DFDFE803608BE6721D9074C11`가 main `7919f58d10f512de68e32ac80a3cf2765276502a` 기반 20260921-150419-3b6c4768 릴리스 APK와 일치했습니다. 이전 날짜의 결과를 이번 버전의 검증으로 취급하지 않습니다.
- Text: 상하 스크롤로 Letter Spacing / Line Spacing 도달. Effects: 스크롤 후 Background Effect / No Effect 위치 이동 확인. Background: 기본 화면에서는 내용이 모두 들어가며, 임시 1080×1600 조건에서 스크롤하여 Blur에 도달했습니다.
- Play 진입·뒤로 가기, Settings 진입·V1.1.2 표시, 보상 모달 열기·닫기, 콜드 재시작 및 홈 이동 후 복귀를 확인했습니다. 원본 두 줄 입력과 speed 30 / size 50이 유지되었습니다. 복원된 메인 화면의 시스템 바 숨김을 확인했습니다.
- 콜드 Activity 시작 명령은 성공했고 TotalTime 1433 ms였습니다. 이는 메인 준비 완료 시간이나 스플래시 전환의 프레임별 무결성을 측정한 값이 아닙니다. 현재 프로세스 logcat에서 FATAL EXCEPTION / ANR / Fatal signal / 지정 JS 예외 패턴은 0건이었습니다. 과거 전체 실행 이력에 대한 무충돌 보장은 아닙니다.
- `node --test scripts/qa-ad-state.cjs`: 26개 통과. 광고 재시도 예산, 방향별 배너 상태 보존, 보상 만료·중복·소스 확인 등의 상태 회귀 검증이며 실제 SDK 노출/보상을 대신하지 않습니다.
- 테스트 기기 등록 여부를 사용자가 알 수 없다고 답했으므로 production 광고 재생·클릭은 하지 않았습니다. 오프라인에서도 이미 준비된 보상 광고 버튼이 활성 상태였으므로 네트워크 차단을 테스트 광고의 증거로 취급하지 않았습니다.
- 미검증: 실제 광고 재생·보상·광고 창 시스템 바, SDK 배너 노출/숨김 시 자동 갱신 및 impression, 실기기 방향 전환 시 배너 재사용, 구매·복원, 잠금 해제 기능 전체, Amplitude 수신, iOS/iPad, 스플래시 프레임별 전환. 전체 출시 QA 완료 판정은 유보합니다.
- 종료 시 해상도 1080×2408, 기존 density override 480, 자동 회전 1 / user_rotation 0, Wi-Fi 켜짐을 복원했습니다. 앱 데이터 초기화·재설치·새 빌드·커밋·푸시는 하지 않았습니다. 이번 범위에서 재현된 제품 결함이 없어 앱 소스를 변경하지 않았습니다.
- 증빙: [2026-09-21 로컬 산출물](../artifacts/qa-20260921-final/)의 UI XML, PNG, current-process.log, state-regression.log. 기존 README 및 빌드 최적화 문서의 미커밋 빌드 기록은 보존했습니다.

### 같은 날 추가 QA — 테스트 기기 등록 후

사용자가 AdMob 테스트 기기 등록 완료를 알린 뒤 앱을 force-stop하고 새로 실행했습니다. Settings 배너는 표시됐지만 저장한 화면에서 Test Ad 라벨이 확인되지 않았고, SDK 로그도 테스트 기기 설정 안내만 제공했습니다. 등록 화면과 실제 요청의 테스트 처리 여부는 구분합니다. 테스트 광고임을 확인하지 못한 상태에서 보상 광고를 재생하거나 광고 소재를 클릭하지 않았습니다. Google은 신규 테스트 기기 반영이 보통 15분 이내, 최대 24시간 걸릴 수 있다고 안내합니다: https://developers.google.com/admob/android/test-ads . 따라서 실제 재생/닫기/보상 지급/보상 후 잠금 해제 검증은 남아 있습니다.

- Watch Ad 준비 완료·활성 버튼 및 모달 닫기를 확인했습니다. 준비 상태는 보상 지급 성공이 아닙니다.
- 구매: README의 기존 승인대로 Upgrade to Pro 메뉴와 premium 라우트가 출시 앱에서 제외되어 있습니다. 구매 UI를 재활성화하거나 결제하지 않았습니다. ApiClient의 서명·소유 확인, acknowledge, 중복 구매 차단, 수동 복원 구현을 읽었으나 실제 결제/취소/pending/복원은 미검증입니다.
- Perfetto 각 30초, 기존 영어 두 줄 / speed 30 / size 50: 미리보기 프레임 시작 119.88회/초, 간격 P95 8.542 ms, 완료 기한 초과 7건; Play 120.08회/초, 간격 P95 8.575 ms, 완료 기한 초과 1건. 시작 빈도는 실제 표시 FPS가 아닙니다. Play 측정에서는 해당 화면 레이어 하나만 기록됐습니다.
- 측정 전 PSS는 미리보기 379.45 MiB, Play 520.99 MiB였습니다. 단회 표본이며 누수나 개선율을 판단할 수 없습니다. 이전 날짜와 광고 캐시·입력·실행 조건이 달라 직접 성능 개선 비교를 하지 않습니다.
- 현재 프로세스 로그의 지정 fatal/ANR/JS 예외 패턴 0건. 앱을 메인 화면으로 돌려놓았으며 입력·설정·구매 권한을 변경하지 않았습니다. 앱 소스 수정·새 빌드·커밋·푸시는 하지 않았습니다.
- 증빙: [추가 QA 산출물](../artifacts/qa-20260921-testads/)의 settings.png, reward-ready.xml, performance-summary.json, Perfetto trace/CSV 및 프로세스 로그.

### 테스트 모드 반영 후 실제 보상 흐름 검증

후속 요청에서 Test Ad로 전환해 검증하도록 승인받았습니다. 새로 실행한 기존 V1.1.2 (30) APK의 Settings 배너에서 Test Ad 라벨을 확인했고, 이어 보상 광고에서도 Test Ad 라벨을 확인했습니다. 콘솔 등록이 반영되어 소스의 production 광고 설정 변경이나 QA APK 재빌드 없이 진행했습니다. 앞의 미검증 기록은 해당 시점의 결과이며 아래 항목으로 보완합니다.

- Watch Ad 탭 → 테스트 보상 광고 표시 → 두 번째 광고 완료 화면의 X 닫기 → 메인 복귀를 실제 기기에서 확인했습니다. 광고 소재/CTA는 클릭하지 않았습니다.
- 복귀 후 프리셋 2~5 및 Size의 잠금 표시가 제거됐습니다. Size + 버튼으로 50→51 변경 후 − 버튼으로 50 복원했습니다. UI 표시뿐 아니라 이전 유료 제한 컨트롤이 실제 작동했습니다.
- 앱 force-stop/콜드 재시작 후에도 Size 50→51 변경이 가능했고 다시 50으로 복원했습니다. 보상 권한의 재시작 유지가 확인됐습니다. SDK 이벤트 원본/정확한 지급 횟수를 release 로그에서 직접 측정한 것은 아니며, UI 및 기능 결과를 확인한 것입니다.
- 정상 SDK 보상을 임의로 삭제하지 않았습니다. 보상은 기존 구현의 2시간 임시 Pro 권한이며, 실제 2시간 경과 후 만료는 이번 점검에서 기다려 검증하지 않았습니다. 구매 권한과 별개입니다.
- 증빙: 같은 추가 QA 산출물 디렉터리의 test-ad-open.xml, test-ad-complete.xml, test-reward-late.png, after-reward.png/xml, unlocked-size-plus.xml, reward-restart.xml, restart-size-plus.xml. 테스트 광고의 성공을 production 공급률/수익 검증으로 해석하지 않습니다. iOS·광고 조기 종료·만료·중복 이벤트 실기기 검증은 남아 있습니다.

### 만료·iOS 확인 및 이번 전달 범위

2026-09-21 후속 확인에서 Apple iPhone USB 연결은 감지됐지만, 이 Windows 환경에 iOS 앱 조작 도구가 없어 iOS 실기기 검증은 수행하지 못했습니다. 실제 2시간 만료도 완료 증거가 없으므로 미검증을 유지합니다. 19:35의 1회 확인은 예약 제안 카드를 생성한 상태이며 실행·완료로 기록하지 않습니다.

이번 사용자 요청의 main 전달에는 이전 빌드 기록, Android 실기기·Test Ad·성능 QA 기록, README의 현재 검증 상태 갱신과 내부 전용 심볼 세 개의 export 정리를 포함합니다. 로컬 artifacts의 APK/AAB·로그·스크린샷·트레이스는 기존 제외 정책대로 보존하며 원본을 Git에 추가하지 않습니다. 이번 문서화·전달 단계에서 빌드·린트·테스트는 실행하지 않았습니다.

## 2026-09-22 — 최신 V1.1.2 (30) 설치 APK 최종 QA

- 대상: Galaxy SM-M336K / Android 16 / 1080×2408 / density override 480. 설치 `base.apk` SHA-256 `4F800A29E3BCB169F784996EBFD80F8884CE743049DF58FB2F4810ACBBEF3261`가 main `a49fed6e711156b841a486528dc63241baae414c` 기반 `LedPopV112.apk`와 일치했습니다. production App ID·광고 단위를 유지한 빌드이며 등록된 테스트 기기에서 배너·보상 광고의 `Test Ad` 표시를 확인했습니다.
- 이전 테스트 보상 후 2시간을 넘겨 다시 실행했을 때 프리셋 2~5와 Size가 잠긴 상태로 복귀했습니다. 이는 실제 경과 후 만료를 검증한 결과이며 정확히 2시간 경계 순간을 측정한 것은 아닙니다. 새 테스트 광고에서 `Reward granted`를 확인하고 닫은 뒤 잠금이 해제됐으며, Size 50→51 동작과 50 복원, force-stop/콜드 재시작 후 권한 유지도 확인했습니다. 광고 소재·CTA는 클릭하지 않았습니다.
- Text는 Letter Spacing/Line Spacing까지, Effects는 Background Effect까지 상하 스크롤했습니다. Background 기본 콘텐츠, Play 진입·복귀, Settings V1.1.2, 시스템 바 숨김, 탭/스크롤 상태 유지와 기본 입력·speed 30·size 50 복원을 확인했습니다.
- 콜드 Activity `TotalTime`은 1098 ms였습니다. 이는 첫 화면의 시각적 준비 완료나 스플래시 전환 프레임을 뜻하지 않습니다. 30초 Perfetto 측정에서 미리보기/Play의 프레임 시작 빈도는 각각 120.08/s였고 시작 간격 P95는 8.684/8.559 ms였습니다. Play 재측정도 120.08/s, 8.561 ms였습니다. Play 두 회의 FrameTimeline 분류는 모두 `None`이었고, 단회 PSS는 미리보기 329.85 MiB, Play 463.04/489.77 MiB였습니다. 이 표본만으로 누수나 개선율을 판단하지 않습니다.
- 현재 앱 프로세스 로그에서 FATAL EXCEPTION / ANR / Fatal signal / 지정 React Native JS 예외 패턴은 0건이었습니다. 종료 시 Wi-Fi 켜짐, 자동 회전 1 / user_rotation 0, 기본 두 줄 입력과 size 50을 확인했습니다.
- `node --test scripts/qa-ad-state.cjs`: 26개 통과. `npx tsc --noEmit`: 성공. 새 APK/AAB 빌드는 실행하지 않았습니다.

### 발견한 접근성 문제와 수정

보상 모달이 열린 동안 UIAutomator 접근성 트리에 뒤쪽 메인 화면의 프리셋·Settings·탭 컨트롤이 남아 있었습니다. `app/index.tsx`는 모달 표시 중 메인 콘텐츠 하위 요소를 접근성 탐색에서 숨기고, `components/rewardAdModal.tsx`는 표시 중인 모달을 접근성 모달로 선언하도록 수정했습니다. 시각 배치, 터치 차단, 광고 요청·표시·보상 로직은 바꾸지 않았습니다.

이 수정은 TypeScript 및 상태 회귀 테스트를 통과했지만 설치 APK에는 포함되지 않았습니다. 따라서 TalkBack 포커스 격리의 Android 실기기 확인과 iPhone/iPad 동작은 새 네이티브 빌드에서 미검증입니다. 증빙은 [2026-09-22 로컬 QA 산출물](../artifacts/qa-20260922-final/)에 보존했습니다.
